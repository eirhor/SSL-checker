# SSL Checker

Simple console application that reads a bulk of website url's from a .txt file, and process them using SSL Labs, and return the site + grade into a CSV file. Delimiter in .txt file and CSV file is ";".